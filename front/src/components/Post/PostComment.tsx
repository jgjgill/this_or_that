import styles from './postComment.module.scss'

interface PostCommentProps {
  postCommentData: []
}

const PostComment = ({ postCommentData }: PostCommentProps) => {
  return (
    <div>
      <div>postComment</div>
    </div>
  )
}

export default PostComment
